# .git is deliberately NOT ignored: `go build` stamps vcs.revision/vcs.time into
# the binary only when it can see the repository, and /versionz reports them.
aidlc-docs
*.md
deploy/helm
github
.aidlc-rule-details
