apiVersion: v1
kind: Service
metadata:
  name: {{ include "kudzu.fullname" . }}
  labels:
    {{- include "kudzu.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  selector:
    {{- include "kudzu.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
